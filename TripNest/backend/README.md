# TripNest Backend

This is the backend for the TripNest iOS app. It is built with FastAPI.

## Setup

1. Install the dependencies:

   ```bash
   pip install -r requirements.txt
   ```

2. Run the development server:

   ```bash
   uvicorn main:app --reload
   ```
